## To run the application

1) Clone the repository using `git clone https://github.com/shivasaxena/PMS7003-UI.git`
2) Install Graphics libraries `sudo apt-get install golang gcc libgl1-mesa-dev xorg-dev libxkbcommon-dev`
3) cd in to directory using `cd PMS7003-UI`
4) run the command `go build`
